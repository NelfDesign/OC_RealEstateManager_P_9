<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="fr.nelfdesign.oc_realestatemanager_p_9">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <!-- Permission used by the playStore to check if the phone user has a camera -->
    <uses-feature android:name="android.hardware.camera" android:required="true"/>

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".ui.activity.ConnexionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.MainActivity"
            android:theme="@style/addProperty"
            android:windowSoftInputMode="adjustPan" />

        <activity android:name=".ui.activity.DetailProperty" />

        <activity android:name=".ui.activity.AddPropertyActivity"
            android:theme="@style/addProperty"/>

        <provider
            android:authorities="fr.nelfdesign.oc_realestatemanager_p_9.provider"
            android:name=".provider.PropertyContentProvider"
            android:exported="true"/>

        <receiver android:name=".notification.NotificationReceiver"/>
    </application>

</manifest>